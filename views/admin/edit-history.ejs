<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= title %></title>
    <link href="https://cdn.jsdelivr.net/npm/tailwindcss@2.2.19/dist/tailwind.min.css" rel="stylesheet">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/5.15.4/css/all.min.css">
    <link rel="stylesheet" href="/css/styles.css">
    <link rel="stylesheet" href="/css/about.css">
    <style>
        .wysiwyg-input, .wysiwyg-textarea { 
            background-color: rgba(239, 246, 255, 0.3); 
            border: 1px dashed rgba(0, 0, 0, 0.2); 
            padding: 2px 4px; 
            margin: 0 0 2px 0; 
            display: inline-block; 
            width: auto; 
            min-width: 100px; 
            border-radius: 3px; 
            color: inherit; 
            font-size: inherit; 
            font-weight: inherit; 
            line-height: inherit; 
            vertical-align: baseline; 
        }
        .wysiwyg-textarea { 
            width: 100%; 
            min-height: 60px; 
            display: block; 
            resize: vertical; 
        }
        .wysiwyg-file-input-container { 
            border: 1px dashed rgba(0, 0, 0, 0.2); 
            padding: 10px; 
            margin-top: 10px; 
            border-radius: 5px; 
            background-color: rgba(239, 246, 255, 0.3); 
        }
        .wysiwyg-file-input-container img.preview-image { 
            max-height: 150px; 
            margin-bottom: 5px; 
            border: 1px solid #eee; 
            border-radius: 3px; 
            display: inline-block; 
            vertical-align: middle; 
            margin-right: 10px; 
        }
        .wysiwyg-file-input { 
            display: inline-block; 
            width: auto; 
            font-size: 0.9em; 
            vertical-align: middle; 
        }
        .wysiwyg-input:hover, .wysiwyg-textarea:hover, .wysiwyg-file-input-container:hover { 
            border: 1px dashed #4f46e5; 
            background-color: rgba(224, 231, 255, 0.4); 
        }
        .wysiwyg-file-input::file-selector-button { 
            font-size: 0.8em; 
            padding: 4px 8px; 
            margin-right: 5px; 
            border-radius: 3px; 
            border: 1px solid #ccc; 
            background-color: #f0f0f0; 
            cursor: pointer; 
        }
        #save-changes-btn { 
            position: fixed; 
            bottom: 20px; 
            right: 20px; 
            z-index: 100; 
        }
        .timeline-event-edit { 
            position: relative; 
            padding-left: 2.5rem; /* Space for the timeline marker and line */
            padding-right: 40px; /* Space for remove button */
            margin-bottom: 2rem; 
        }
        .remove-item-btn { 
            position: absolute; 
            top: 1rem; 
            right: 1rem; 
            background-color: rgba(255, 0, 0, 0.6); 
            color: white; 
            border-radius: 50%; 
            width: 22px; 
            height: 22px; 
            display: flex; 
            align-items: center; 
            justify-content: center; 
            font-size: 0.8em; 
            cursor: pointer; 
            border: none; 
            line-height: 0; 
        }
        .remove-item-btn:hover { 
            background-color: rgba(255, 0, 0, 0.8); 
        }
        .add-item-container { 
            text-align: center; 
            padding: 15px; 
            border-top: 1px solid #eee; 
            margin-top: 15px; 
        }
        /* Timeline styling */
        .timeline-container {
            position: relative;
        }
        .timeline-container::before {
            content: '';
            position: absolute;
            left: 1rem; /* Align with the center of the marker */
            top: 0;
            bottom: 0;
            width: 2px;
            background: #e2e8f0; /* Gray-200 */
        }
        .timeline-event-edit::before {
            content: '';
            position: absolute;
            left: 0.75rem; /* Center the marker on the line */
            top: 1.5rem;
            width: 0.75rem;
            height: 0.75rem;
            background: #4f46e5; /* Indigo-600 */
            border-radius: 50%;
            border: 2px solid #fff;
            box-shadow: 0 0 0 3px #e2e8f0; /* Gray-200 ring */
        }
        .admin-nav-scroll {
            overflow-y: auto;
            overflow-x: hidden;
            padding-bottom: 1rem;
        }
        .admin-nav-scroll::-webkit-scrollbar {
            width: 6px;
        }
        .admin-nav-scroll::-webkit-scrollbar-track {
            background: rgba(67, 56, 202, 0.2);
            border-radius: 10px;
        }
        .admin-nav-scroll::-webkit-scrollbar-thumb {
            background-color: rgba(79, 70, 229, 0.7);
            border-radius: 10px;
            border: 1px solid transparent;
            background-clip: content-box;
        }
        .admin-nav-scroll::-webkit-scrollbar-thumb:hover {
            background-color: rgba(67, 56, 202, 1);
        }
    </style>
</head>
<body class="bg-gray-100">
    <div class="flex h-screen">
        <%- include('partials/sidebar', { currentPage: currentPage }) %>

        <div class="flex-1 overflow-y-auto">
            <% if (typeof historyData === 'undefined' || !historyData) { %>
                <div class="p-10 bg-red-100 border border-red-400 text-red-700" role="alert">Error loading history data.</div>
            <% } else { %>
                <form action="/admin/edit-history" method="POST" enctype="multipart/form-data" id="edit-history-form">
                    <main>
                        <section class="about-hero bg-cover bg-center h-80 md:h-96 text-white flex items-center relative" style="background-image: url('<%= historyData.hero?.backgroundImage ?? '/images/about-hero.jpg' %>')">
                            <div class="absolute inset-0 bg-gradient-to-r from-blue-900/80 to-indigo-900/80 z-10"></div>
                            <div class="container mx-auto text-center relative z-20 px-4">
                                <h1 class="text-4xl md:text-5xl font-bold mb-4">
                                    <input type="text" name="hero.title" value="<%= historyData.hero?.title ?? '' %>" class="wysiwyg-input text-center text-4xl md:text-5xl font-bold text-white bg-transparent border-dashed border-gray-400 hover:border-white focus:border-white focus:ring-0" style="width: 80%;">
                                </h1>
                                <p class="text-lg md:text-xl max-w-3xl mx-auto">
                                    <textarea name="hero.subtitle" rows="2" class="wysiwyg-textarea text-center text-lg md:text-xl text-white bg-transparent border-dashed border-gray-400 hover:border-white focus:border-white focus:ring-0 w-full max-w-3xl mx-auto"><%= historyData.hero?.subtitle ?? '' %></textarea>
                                </p>
                                <div class="mt-8 max-w-md mx-auto">
                                    <div class="wysiwyg-file-input-container bg-black bg-opacity-30 text-left">
                                        <label class="block text-sm font-medium text-gray-200 mb-1" for="heroBackgroundImage">Edit Hero Background Image</label>
                                        <% if (historyData.hero?.backgroundImage) { %>
                                            <div class="mb-1 inline-block"><img src="<%= historyData.hero.backgroundImage %>" alt="Current Hero Background" class="preview-image"></div>
                                        <% } %>
                                        <input type="file" id="heroBackgroundImage" name="heroBackgroundImage" accept="image/*" class="wysiwyg-file-input text-gray-300">
                                        <p class="mt-1 text-xs text-gray-400 block">Upload new image. Leave empty to keep current.</p>
                                    </div>
                                </div>
                            </div>
                        </section>

                        <section class="py-16 md:py-20 bg-gray-50">
                            <div class="container mx-auto px-4">
                                <div class="max-w-3xl mx-auto">
                                    <div class="text-center mb-16">
                                        <i class="fas fa-scroll text-4xl text-indigo-600 mb-4"></i>
                                        <h2 class="text-3xl md:text-4xl font-bold text-gray-800 mb-4">
                                            <input type="text" name="content.title" value="<%= historyData.content?.title ?? '' %>" class="wysiwyg-input text-center text-3xl md:text-4xl font-bold w-full">
                                        </h2>
                                        <div class="w-20 h-1 bg-indigo-500 mx-auto"></div>
                                    </div>

                                    <%# Editable Timeline %>
                                    <div id="timeline-container" class="timeline-container">
                                        <% if(historyData.content?.timelineEvents && historyData.content.timelineEvents.length > 0) { %>
                                            <% historyData.content.timelineEvents.forEach((event, index) => { %>
                                                <div class="timeline-event-edit">
                                                    <div class="grid grid-cols-1 md:grid-cols-4 gap-4 mb-2">
                                                        <div class="md:col-span-1">
                                                            <label class="block text-xs font-medium text-gray-700" for="event-year-<%= index %>">Year</label>
                                                            <input id="event-year-<%= index %>" type="text" name="events[<%= index %>].year" value="<%= event.year ?? '' %>" placeholder="e.g., 2010" class="wysiwyg-input text-lg w-full">
                                                        </div>
                                                        <div class="md:col-span-3">
                                                            <label class="block text-xs font-medium text-gray-700" for="event-title-<%= index %>">Title</label>
                                                            <input id="event-title-<%= index %>" type="text" name="events[<%= index %>].title" value="<%= event.title ?? '' %>" placeholder="Milestone Title" class="wysiwyg-input text-xl font-semibold w-full">
                                                        </div>
                                                    </div>
                                                    <div>
                                                        <label class="block text-xs font-medium text-gray-700" for="event-desc-<%= index %>">Description</label>
                                                        <textarea id="event-desc-<%= index %>" name="events[<%= index %>].description" rows="3" class="wysiwyg-textarea text-gray-600 w-full"><%= event.description ?? '' %></textarea>
                                                    </div>
                                                    <button type="button" class="remove-item-btn" data-index="<%= index %>"><i class="fas fa-times"></i></button>
                                                </div>
                                            <% }) %>
                                        <% } else { %>
                                            <p class="text-center text-gray-500 italic">No history events defined yet. Click "Add Event" to begin.</p>
                                        <% } %>
                                    </div>
                                    <div class="add-item-container">
                                        <button type="button" id="add-event-btn" class="bg-green-600 text-white py-2 px-4 rounded-md hover:bg-green-700 text-sm">
                                            <i class="fas fa-plus mr-1"></i> Add Timeline Event
                                        </button>
                                    </div>
                                </div>
                            </div>
                        </section>
                    </main>

                    <button type="submit" id="save-changes-btn" class="bg-indigo-600 text-white py-3 px-6 rounded-lg shadow-lg hover:bg-indigo-700 focus:outline-none focus:ring-2 focus:ring-offset-2 focus:ring-indigo-500 transition transform hover:scale-105">
                        <i class="fas fa-save mr-2"></i> Save Changes
                    </button>
                </form>
            <% } %>
        </div>
    </div>

    <script>
        document.addEventListener('DOMContentLoaded', () => {
            const eventsContainer = document.getElementById('timeline-container');
            const addEventBtn = document.getElementById('add-event-btn');

            const updateEventIndices = () => {
                const eventItems = eventsContainer.querySelectorAll('.timeline-event-edit');
                const eventCount = eventItems.length;
                eventItems.forEach((item, index) => {
                    const yearInput = item.querySelector('input[name^="events"][name$=".year"]');
                    const titleInput = item.querySelector('input[name^="events"][name$=".title"]');
                    const descTextarea = item.querySelector('textarea[name^="events"][name$=".description"]');
                    const yearLabel = item.querySelector('label[for^="event-year-"]');
                    const titleLabel = item.querySelector('label[for^="event-title-"]');
                    const descLabel = item.querySelector('label[for^="event-desc-"]');

                    if(yearInput) yearInput.name = `events[${index}].year`;
                    if(yearInput) yearInput.id = `event-year-${index}`;
                    if(yearLabel) yearLabel.setAttribute('for', `event-year-${index}`);

                    if(titleInput) titleInput.name = `events[${index}].title`;
                    if(titleInput) titleInput.id = `event-title-${index}`;
                    if(titleLabel) titleLabel.setAttribute('for', `event-title-${index}`);

                    if(descTextarea) descTextarea.name = `events[${index}].description`;
                    if(descTextarea) descTextarea.id = `event-desc-${index}`;
                    if(descLabel) descLabel.setAttribute('for', `event-desc-${index}`);

                    const removeBtn = item.querySelector('.remove-item-btn');
                    if(removeBtn) removeBtn.dataset.index = index;
                });
                const noEventsMsg = document.querySelector('#timeline-container > p.text-gray-500');
                if (noEventsMsg) noEventsMsg.style.display = eventCount === 0 ? 'block' : 'none';
            };

            if(addEventBtn && eventsContainer) {
                addEventBtn.addEventListener('click', () => {
                    const currentEventCount = eventsContainer.querySelectorAll('.timeline-event-edit').length;

                    const newEventItem = document.createElement('div');
                    newEventItem.className = 'timeline-event-edit';
                    newEventItem.innerHTML = `
                        <div class="grid grid-cols-1 md:grid-cols-4 gap-4 mb-2">
                            <div class="md:col-span-1">
                                <label class="block text-xs font-medium text-gray-700" for="event-year-${currentEventCount}">Year</label>
                                <input id="event-year-${currentEventCount}" type="text" name="events[${currentEventCount}].year" value="" placeholder="e.g., 2025" class="wysiwyg-input text-lg w-full">
                            </div>
                            <div class="md:col-span-3">
                                <label class="block text-xs font-medium text-gray-700" for="event-title-${currentEventCount}">Title</label>
                                <input id="event-title-${currentEventCount}" type="text" name="events[${currentEventCount}].title" value="" placeholder="Milestone Title" class="wysiwyg-input text-xl font-semibold w-full">
                            </div>
                        </div>
                        <div>
                            <label class="block text-xs font-medium text-gray-700" for="event-desc-${currentEventCount}">Description</label>
                            <textarea id="event-desc-${currentEventCount}" name="events[${currentEventCount}].description" rows="3" placeholder="Description..." class="wysiwyg-textarea text-gray-600 w-full"></textarea>
                        </div>
                        <button type="button" class="remove-item-btn" data-index="${currentEventCount}"><i class="fas fa-times"></i></button>
                    `;
                    const noEventsMsg = eventsContainer.querySelector('p.text-gray-500');
                    if (noEventsMsg) noEventsMsg.remove();

                    eventsContainer.appendChild(newEventItem);
                    updateEventIndices();
                });
            }

            if(eventsContainer) {
                eventsContainer.addEventListener('click', (e) => {
                    const removeBtn = e.target.closest('.remove-item-btn');
                    if (removeBtn) {
                        if (confirm('Are you sure you want to remove this timeline event?')) {
                            removeBtn.closest('.timeline-event-edit').remove();
                            updateEventIndices();
                        }
                    }
                });
            }
            updateEventIndices();
        });
    </script>
</body>
</html>