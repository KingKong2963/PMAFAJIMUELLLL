<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= typeof title !== 'undefined' ? title : 'Admin Dashboard' %></title>
    <link href="https://cdn.jsdelivr.net/npm/tailwindcss@2.2.19/dist/tailwind.min.css" rel="stylesheet">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/5.15.4/css/all.min.css">
    <link rel="stylesheet" href="/css/styles.css">
    <style>
        /* More pronounced hover effect for cards */
        .dashboard-card {
            transition: transform 0.2s ease-in-out, box-shadow 0.3s ease-in-out;
            border: 1px solid #e5e7eb; /* Softer border */
        }
        .dashboard-card:hover {
            transform: translateY(-5px);
            box-shadow: 0 12px 20px -3px rgba(0, 0, 0, 0.1), 0 4px 8px -2px rgba(0, 0, 0, 0.06);
        }
        /* Quick Link Button Style */
        .quick-link {
            display: flex;
            flex-direction: column;
            align-items: center;
            justify-content: center;
            padding: 1rem;
            border-radius: 0.75rem;
            background-color: white;
            border: 1px solid #e5e7eb;
            text-align: center;
            transition: all 0.2s ease-in-out;
            color: #4b5563; /* gray-600 */
        }
        .quick-link:hover {
            background-color: #f9fafb; /* gray-50 */
            border-color: #d1d5db; /* gray-300 */
            color: #1f2937; /* gray-800 */
            transform: translateY(-3px);
            box-shadow: 0 4px 10px rgba(0,0,0,0.05);
        }
        .quick-link i {
            font-size: 1.75rem; /* text-3xl approx */
            margin-bottom: 0.75rem;
            color: #4f46e5; /* indigo-600 */
        }
        .quick-link span {
            font-size: 0.9rem;
            font-weight: 500;
        }
        /* Barangay President List Item */
        .barangay-list-item {
            display: flex;
            justify-content: space-between;
            align-items: center;
            padding: 0.75rem 1rem;
            border-bottom: 1px solid #f3f4f6; /* gray-100 */
            transition: background-color 0.15s ease;
        }
        .barangay-list-item:last-child {
            border-bottom: none;
        }
        .barangay-list-item:hover {
            background-color: #f9fafb; /* gray-50 */
        }
         .barangay-list-item .name {
            font-weight: 500;
            color: #1e3a8a; /* blue-800 */
         }
          .barangay-list-item .president {
            color: #4b5563; /* gray-600 */
            font-size: 0.9em;
          }
           .barangay-list-item .edit-link {
            font-size: 0.8em;
            color: #4f46e5; /* indigo-600 */
            font-weight: 500;
            opacity: 0; /* Hidden by default */
            transition: opacity 0.2s ease;
           }
            .barangay-list-item:hover .edit-link {
                opacity: 1; /* Show on hover */
            }

    </style>
</head>
<body class="bg-gray-100">
    <div class="flex h-screen">
        <%# Include the sidebar partial %>
        <%- include('partials/sidebar', { currentPage: currentPage }) %>

        <div class="flex-1 p-6 md:p-8 lg:p-10 overflow-y-auto">
            <h1 class="text-3xl font-bold text-gray-800 mb-2">Admin Dashboard</h1>
            <p class="text-gray-600 mb-8">Welcome back! Here's a summary of your site content.</p>

            <% if (typeof stats !== 'undefined') { %>
            <div class="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-4 gap-6 mb-8">
                <div class="dashboard-card bg-white p-6 rounded-xl shadow-md flex items-center space-x-4">
                    <div class="bg-red-100 text-red-600 p-3 rounded-full">
                        <i class="fas fa-user-tie fa-lg"></i> 
                    </div>
                    <div>
                        <p class="text-sm font-medium text-gray-500">Mayor</p>
                        <p class="text-2xl font-bold text-gray-800"><%= stats.mayorCount %></p>
                    </div>
                </div>
                <div class="dashboard-card bg-white p-6 rounded-xl shadow-md flex items-center space-x-4">
                    <div class="bg-blue-100 text-blue-600 p-3 rounded-full">
                        <i class="fas fa-landmark fa-lg"></i> 
                    </div>
                    <div>
                        <p class="text-sm font-medium text-gray-500">President</p>
                        <p class="text-2xl font-bold text-gray-800"><%= stats.presidentCount %></p>
                    </div>
                </div>
                 <div class="dashboard-card bg-white p-6 rounded-xl shadow-md flex items-center space-x-4">
                    <div class="bg-purple-100 text-purple-600 p-3 rounded-full">
                        <i class="fas fa-users fa-lg"></i> 
                    </div>
                    <div>
                        <p class="text-sm font-medium text-gray-500">Team Members</p>
                        <p class="text-2xl font-bold text-gray-800"><%= stats.teamMemberCount %></p>
                    </div>
                </div>
                <div class="dashboard-card bg-white p-6 rounded-xl shadow-md flex items-center space-x-4">
                    <div class="bg-green-100 text-green-600 p-3 rounded-full">
                        <i class="fas fa-map-marked-alt fa-lg"></i> 
                    </div>
                    <div>
                        <p class="text-sm font-medium text-gray-500">Managed Barangays</p>
                        <p class="text-2xl font-bold text-gray-800"><%= stats.barangayCount %></p>
                    </div>
                </div>
            </div>

            <div class="mb-8">
                <h2 class="text-xl font-semibold text-gray-700 mb-4">Quick Actions</h2>
                <div class="grid grid-cols-2 md:grid-cols-3 lg:grid-cols-6 gap-4">
                    <a href="/admin/edit-home" class="quick-link">
                        <i class="fas fa-home"></i>
                        <span>Edit Home</span>
                    </a>
                     <a href="/admin/edit-leadership" class="quick-link">
                        <i class="fas fa-users"></i>
                        <span>Edit Leadership</span>
                    </a>
                     <a href="/admin/edit-barangay" class="quick-link">
                        <i class="fas fa-map-marked-alt"></i>
                        <span>Edit Barangays</span>
                    </a>
                    <a href="/admin/edit-services" class="quick-link">
                        <i class="fas fa-concierge-bell"></i>
                        <span>Edit Services</span>
                    </a>
                     <a href="/admin/edit-gallery" class="quick-link">
                        <i class="fas fa-images"></i>
                        <span>Edit Gallery</span>
                    </a>
                    <a href="/admin/edit-contact" class="quick-link">
                        <i class="fas fa-envelope"></i>
                        <span>Edit Contact</span>
                    </a>
                </div>
            </div>


            <div class="grid grid-cols-1 lg:grid-cols-3 gap-8">
                 
                <div class="lg:col-span-1 bg-white p-6 rounded-xl shadow-lg dashboard-card">
                    <h2 class="text-xl font-semibold text-gray-700 mb-4">Leadership Role Distribution</h2>
                    <div class="relative h-64 md:h-72"> 
                        <canvas id="leadershipBreakdownChart"></canvas>
                    </div>
                </div>

                
                <div class="lg:col-span-2 bg-white p-6 rounded-xl shadow-lg dashboard-card">
                    <div class="flex justify-between items-center mb-4">
                        <h2 class="text-xl font-semibold text-gray-700">Barangay Presidents</h2>
                         <a href="/admin/edit-barangay" class="text-sm font-medium text-indigo-600 hover:text-indigo-800">
                            Manage All Barangays &rarr;
                        </a>
                    </div>
                    <div class="max-h-80 overflow-y-auto"> 
                        <% if (typeof barangayList !== 'undefined' && barangayList.length > 0) { %>
                           <div class="divide-y divide-gray-100"> 
                                <% barangayList.forEach(barangay => { %>
                                    <div class="barangay-list-item">
                                        <div>
                                            <span class="name"><%= barangay.name ?? 'N/A' %></span>
                                            <span class="block president">
                                                <i class="fas fa-user-tie text-xs text-gray-400 mr-1"></i>
                                                <%= barangay.officers?.president ?? 'President Not Set' %>
                                            </span>
                                        </div>
                                        <a href="/admin/edit-barangay#barangays" class="edit-link">Edit</a> 
                                    </div>
                                <% }); %>
                            </div>
                        <% } else { %>
                            <p class="px-4 py-4 text-center text-sm text-gray-500">No barangay data available.</p>
                        <% } %>
                    </div>
                </div>
            </div>
            <% } else { %>
                <p class="text-center text-red-500 bg-red-100 p-4 rounded-lg">Could not load dashboard statistics.</p>
            <% } %>

        </div>
    </div>

   
    <script src="https://cdn.jsdelivr.net/npm/chart.js@3.9.1/dist/chart.min.js"></script>
    <script>
      document.addEventListener('DOMContentLoaded', () => {
        <% if (typeof stats !== 'undefined') { %>
          const leadershipCtx = document.getElementById('leadershipBreakdownChart')?.getContext('2d');
          if (leadershipCtx) {
            new Chart(leadershipCtx, {
              type: 'doughnut',
              data: {
                labels: ['Mayor', 'President', 'Team Members'],
                datasets: [{
                  label: 'Leadership Roles',
                  data: [<%= stats.mayorCount %>, <%= stats.presidentCount %>, <%= stats.teamMemberCount %>],
                  backgroundColor: ['#DC2626', '#2563EB', '#7C3AED'], // Red, Blue, Purple
                  borderColor: '#ffffff', // White border for separation
                  borderWidth: 2,
                  hoverOffset: 8,
                  hoverBorderColor: '#f3f4f6'
                }]
              },
              options: {
                responsive: true,
                maintainAspectRatio: false,
                cutout: '65%', // Makes doughnut thinner
                plugins: {
                  legend: {
                    position: 'bottom',
                    labels: { padding: 15, boxWidth: 12, font: { size: 11 } }
                  },
                  tooltip: {
                     backgroundColor: '#1f2937', // Dark tooltip
                     titleFont: { size: 14 }, bodyFont: { size: 12 },
                     padding: 10, cornerRadius: 4,
                     callbacks: {
                        label: function(context) {
                           let label = context.label || '';
                           if (label) { label += ': '; }
                           if (context.parsed !== null) { label += context.parsed; }
                           return label;
                        }
                     }
                  }
                }
              }
            });
          } else { console.error("Could not find canvas element for leadership chart."); }
        <% } else { %>
           console.warn("Stats data not available for chart initialization.");
        <% } %>
      });
    </script>

</body>
</html>